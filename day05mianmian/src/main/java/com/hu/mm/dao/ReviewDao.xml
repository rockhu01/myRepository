<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hu.mm.dao.ReviewDao">
   <!-- 增加审核记录  void add(ReviewLog reviewLog);-->
    <insert id="add" parameterType="ReviewLog">
        insert  into t_review_log (comments,status,question_id,user_id,create_date) values
                (#{comments},#{status},#{questionId},#{userId},#{createDate})
    </insert>

</mapper>
